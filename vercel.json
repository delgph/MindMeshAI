{
  "builds": [
    { "src": "backend/*.py", "use": "@vercel/python" },
    { "src": "agents/*.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/$1" }
  ]
}
